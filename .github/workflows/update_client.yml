name: 🔁 Notificación: Actualización de Cliente Recurrente

on:
  workflow_dispatch:
    inputs:
      client_name:
        description: 'Nombre del cliente recurrente'
        required: true
      new_request:
        description: 'Descripción de la nueva solicitud'
        required: true

jobs:
  notify_team:
    runs-on: ubuntu-latest
    steps:
      - name: 💬 Notificación al equipo sobre actualización
        run: |
          echo "=========================================================="
          echo "⭐ ACCIÓN REQUERIDA: Solicitud de Cliente Recurrente ⭐"
          echo "Cliente: ${{ github.event.inputs.client_name }}"
          echo "Solicitud: ${{ github.event.inputs.new_request }}"
          echo "----------------------------------------------------------"
          echo "Miembros Notificados:"
          echo "- Ana García (Dev): Procesar la nueva solicitud."
          echo "- Carlos Martínez (QA): Realizar pruebas de regresión."
          echo "=========================================================="